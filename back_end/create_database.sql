# Database Creation Script
# Copy this SQL and run in phpMyAdmin:

CREATE DATABASE IF NOT EXISTS petly_db;
USE petly_db;

-- Optional: Show success message
SELECT 'Database petly_db created successfully!' as Message;
